# ----- Frontend (Vite) environment -----
# Copy to `.env` and fill in real values. Never commit the real `.env`.

# Optional external fallback URL for the EHS AI chatbot. The app primarily calls
# the backend at /api/functions/askAI; this is only used as a fallback.
VITE_ASKAI_URL=https://your-askai-endpoint.example.com
